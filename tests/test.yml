---
- hosts: localhost
  become: true
  roles:
    - role: CyVerse-Ansible.rabbitmq-vhost
      rabbitmq_vhost_name: /vhost
      rabbitmq_vhost_users: 
        - name: admin
          configure_priv: .*
          read_priv: .*
          write_priv: .*
          tags:
            - administrator
